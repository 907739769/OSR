-- ----------------------------
-- 20260786: 「订阅管理」(2064) 换成书签图标
--
-- 这条是 20260785 分组时留下的尾巴：该菜单建库时写的是 `fa fa-bookmark-o`（书签），
-- 20260780 把库里 FA 名机械替换成 mdi 时，字典里这一项对到的是
-- `mdi-file-document-outline`（一张文档纸）——字典是按整表批量翻的，对不上语义的
-- 个别项当时没有逐个复核。分组后它和「追剧日历」「热门自动订阅」排在一起，
-- 一张文档纸夹在中间格外突兀。
--
-- 必须按 `menu_id` 改，不能按 icon 值改：`mdi-file-document-outline` 同时还是
-- 「字典管理」(105) 的图标，按值 UPDATE 会连它一起换掉。
--
-- 单开一个脚本而不是改 20260785：后者已经进过 ddl_history，改文件对已升级的库
-- 不会重跑，只会让新库与老库长出两种结果。
-- ----------------------------

UPDATE `sys_menu` SET `icon` = 'mdi-bookmark-outline' WHERE `menu_id` = 2064;
