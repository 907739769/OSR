# OSR (OpenList STRM Relay)

项目知识库维护在 `AGENTS.md`（Claude Code 与 opencode 共用），内容由下方引用自动载入。
**修改请直接改 `AGENTS.md`，不要在本文件里另写一份，避免两边内容漂移。**

根 `AGENTS.md` 只留**全局约定与跨模块纲领**；各业务域的踩坑记录拆在对应目录的 `AGENTS.md` 里，
由同目录 `CLAUDE.md` 按需载入（改到哪个目录才进上下文），清单见根 `AGENTS.md` 的「知识库分片」表。
**新增知识点写进对应分片，不要往根 `AGENTS.md` 里加。**

@AGENTS.md
